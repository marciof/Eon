# Travis CI build config
# https://config.travis-ci.com

language: c

os:
  - linux

compiler:
  - gcc
  - clang

env:
  - CMAKE_BUILD_TYPE=Release
  - CMAKE_BUILD_TYPE=Debug ASAN_OPTIONS="detect_stack_use_after_return=1,print_stats=1"

addons:
  apt:
    packages:
      # Target: all
      - cmake
      - make

      # Target: x86-32
      - nasm # Assembler
      - multiboot # Multiboot header files and `grub-file` to verify compliance

      # Target: x86-32, Script: iso.sh
      - grub-common
      - xorriso

      # Target: x86-32, Script: vm.sh
      # https://www.virtualbox.org/wiki/Linux_Downloads

      # Target: x86-32, Host: Linux 64-bit
      - gcc-multilib # cross-compile 32-bit applications on 64-bit systems

script:
  - cmake "$TRAVIS_BUILD_DIR" && cmake --build .

notifications:
  email:
    on_success: always
